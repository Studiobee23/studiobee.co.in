# Dependencies
node_modules/

# Puppeteer browser binary (dev-only, not needed on server)
.cache/

# Secrets — never commit these
smtp-config.json
.env

# Runtime data — contains customer PII
contacts.json
analytics.json
timelog.json

# Config snapshot (regenerated at runtime from config panel)
# config.json — tracking to serve real content on Vercel

# Uploaded media files
# media/ — tracking media folder to serve videos on Vercel

# Screenshots (dev only)
screenshots/
Phone screenshots/

# Local Claude project data
.claude/

# VS Code workspace settings
.vscode/

# OS files
.DS_Store
Thumbs.db
